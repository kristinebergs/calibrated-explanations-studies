# Calibrated Explanations for Within-Spec Risk Prediction: Uncertainty-Aware Decision Support

## Reproducibility status

This study directory is the canonical home for the Within-Spec paper, but recovery is incomplete.

**Raw experiment result files are not currently present in the GitHub repositories inspected.** They may exist on the authors' local storage and should be added here when recovered.

The paper repository `tuvelofstrom/calibrated-interval-explanations-paper` contains the manuscript and generated benchmark figures, so the experiments were run. However, the current visible branches do not contain the complete experiment runner pipeline. I have therefore not reconstructed or substituted code and labelled it as the historical runner.

The Within-Spec functionality itself is represented in the Calibrated Explanations codebase through probabilistic/thresholded regression with interval thresholds. Historical code that can be tied unambiguously to the paper should be copied into this directory when identified.

## Missing material to recover

- the original experiment runner(s) used for the paper;
- raw per-run/per-dataset result files behind the aggregate tables;
- a fully pinned historical execution environment.

Likely next source: local author storage. Recovered files should be added verbatim, preserving provenance, rather than modernized before archival.
