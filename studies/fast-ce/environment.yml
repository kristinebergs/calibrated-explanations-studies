name: ce-fast-ce
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
    - calibrated-explanations==<VERSION>  # pin to version stated in paper
    - lime==0.2.0.1
    - shap==0.44.0
    - matplotlib
    - seaborn
    - scipy
    - joblib
    - ucimlrepo

