name: ce-calibration-quality
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
    - calibrated-explanations==<VERSION>  # pin to version stated in paper
    - matplotlib
    - scipy
    - scikit-learn

